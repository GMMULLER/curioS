After selecting the keywords output them in the following JSON format:

{
    "keyword1": "keyword1_type"
    "keyword2": "keyword2_type"
    ...
}

The same keyword cannot receive more than one type.

**ONLY OUTPUT THE SPECIFIED JSON NOTHING ELSE. CLASSIFY ALL WORDS OF THE DATAFLOW DESCRIPTION INCLUDING CONECTING WORDS LIKE and, for, to, AND SO ON AS None. DONT LET PONTUATION MARKS AND OTHER CHARACTERS OUT. I NEED TO BE ABLE TO RECONSTRUCT THE TEXT CHARACTER BY CHARACTER FROM YOUR OUTPUT.**

Description of dataflow: